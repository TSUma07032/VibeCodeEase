🎯 消化したTODO:
- 提案のプレビューと適用を分離したコマンドを定義する。

💡 実装内容 (What):
- `vibecodeease.applyIntervention` コマンドを追加し、`vscode.WorkspaceEdit` を用いてテキストの置換（適用）を行う処理を実装。
- `package.json` にコマンドのコントリビューションを追加。
- 適用成功時に `vscode.window.setStatusBarMessage` で完了メッセージを表示。

🔗 活用した基盤:
- `vscode.commands.registerCommand` と `vscode.WorkspaceEdit` のエディタ操作基盤を活用。

📝 次のステップ:
- `package.json` での `keybindings` 設定と `when` 句を活用した条件定義を追加する。
